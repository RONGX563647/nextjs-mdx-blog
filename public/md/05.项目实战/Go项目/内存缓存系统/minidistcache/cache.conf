# MiniDistCache 配置文件
# 键长度限制（字节）
key_len = 64
# 值长度限制（字节）
value_len = 1024
# 最大缓存节点数
cache_size = 1024
# 服务端监听端口
port = 8888
# 定时持久化间隔（秒）
persist_interval = 300
# 日志文件路径
log_file = cache.log
# 持久化文件路径
persist_file = cache.data