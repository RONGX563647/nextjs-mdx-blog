##### 10⚠️ Java Day10 - 异常、泛型、集合

> 💡 **核心提示**：异常处理、泛型和集合是 Java 进阶的核心内容。本文系统讲解异常体系、泛型机制、List 集合的使用，帮你写出更健壮、更通用的代码。

---

#### 快速回顾

- **异常**：程序运行时的错误，Java 用类和对象描述，通过 try-catch 处理
- **泛型**：参数化类型，提供编译时类型安全检查，避免强制类型转换
- **List 集合**：有序、可重复、有索引的集合，常用实现 ArrayList、LinkedList
- **Collections 工具类**：提供排序、查找、同步等常用操作

---

#### 目录

- [一、异常处理](#一异常处理)
  - [1. 异常体系结构](#1-异常体系结构)
  - [2. 异常处理方式](#2-异常处理方式)
  - [3. 自定义异常](#3-自定义异常)
- [二、泛型（Generics）](#二泛型generics)
  - [1. 泛型的作用](#1-泛型的作用)
  - [2. 泛型类](#2-泛型类)
  - [3. 泛型方法](#3-泛型方法)
  - [4. 泛型通配符](#4-泛型通配符)
  - [5. 泛型擦除](#5-泛型擦除)
- [三、List 集合](#三list-集合)
  - [1. List 接口特点](#1-list-接口特点)
  - [2. ArrayList vs LinkedList](#2-arraylist-vs-linkedlist)
  - [3. List 常用方法](#3-list-常用方法)
  - [4. List 排序](#4-list-排序)
- [四、Collections 工具类](#四collections-工具类)
- [问答](#问答)

---

#### 详细内容

##### 一、异常处理

#### 1. 异常体系结构

```
Throwable
├── Error（严重错误，不处理）
│   ├── OutOfMemoryError
│   └── StackOverflowError
└── Exception（可处理）
    ├── RuntimeException（运行时异常，不强制处理）
    │   ├── NullPointerException
    │   ├── ArrayIndexOutOfBoundsException
    │   └── ClassCastException
    └── 其他异常（编译时异常，必须处理）
        ├── IOException
        ├── SQLException
        └── FileNotFoundException
```

**异常体系结构详细图：**

```
┌─────────────────────────────────────────────────────────────┐
│              Java 异常体系结构                  │
│                                                             │
│   ┌─────────────────────────────────────────────┐           │
│   │         Throwable（所有异常的基类）     │           │
│   └──────────────────┬──────────────────────────┘           │
│                      │                                       │
│          ┌───────────┴───────────┐                           │
│          ▼                       ▼                           │
│   ┌──────────────────┐   ┌──────────────────┐           │
│   │     Error       │   │   Exception     │           │
│   │   严重错误       │   │   可处理异常     │           │
│   │   不处理         │   │   需要处理       │           │
│   └────────┬─────────┘   └────────┬─────────┘           │
│            │                      │                       │
│     ┌──────┴──────┐      ┌──────┴──────┐             │
│     ▼             ▼      ▼             ▼               │
│ ┌────────┐  ┌────────┐  ┌──────────────┐  ┌──────────────┐ │
│ │OutOfMem │  │StackOv │  │RuntimeExcep │  │其他异常      │ │
│ │oryError│  │erflowE │  │tion         │  │编译时异常    │ │
│ │内存溢出│  │rror    │  │运行时异常    │  │必须处理      │ │
│ │        │  │栈溢出  │  │不强制处理    │  │IOException  │ │
│ └────────┘  └────────┘  └──────┬───────┘  │SQLException  │ │
│                                │          │FileNotFound │ │
│                          ┌─────────┴─────────┤Exception    │ │
│                          ▼                 └──────────────┘ │
│                   ┌──────────────────┐                      │
│                   │NullPointerException│                     │
│                   │空指针异常        │                     │
│                   └──────────────────┘                      │
│                   ┌──────────────────┐                      │
│                   │ArrayIndexOutOfBounds│                   │
│                   │数组越界异常       │                     │
│                   └──────────────────┘                      │
│                   ┌──────────────────┐                      │
│                   │ClassCastException│                     │
│                   │类型转换异常       │                     │
│                   └──────────────────┘                      │
│                                                             │
│   说明：                                                    │
│   • Error：JVM错误，程序无法恢复（如内存溢出）               │
│   • Exception：程序可处理的异常                               │
│   • RuntimeException：运行时异常，编译器不检查                 │
│   • 其他异常：编译时异常，必须处理（try-catch或throws）       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 2. 异常处理方式

| 方式 | 语法 | 适用场景 |
|------|------|----------|
| try-catch | 捕获并处理 | 知道如何处理异常 |
| try-finally | 必须执行的清理 | 资源释放 |
| try-catch-finally | 捕获+清理 | 完整处理 |
| throws | 抛出给调用者 | 无法处理或不处理 |
| throw | 主动抛出异常 | 业务异常 |

```java
// 基本 try-catch
try {
    int result = 10 / 0;
} catch (ArithmeticException e) {
    System.out.println("除数不能为0");
}

// 多重 catch
try {
    // 可能抛出多种异常
} catch (FileNotFoundException e) {
    System.out.println("文件不存在");
} catch (IOException e) {
    System.out.println("IO错误");
} catch (Exception e) {
    System.out.println("其他错误");
}

// try-catch-finally
try {
    // 可能抛出异常的代码
} catch (Exception e) {
    // 异常处理
} finally {
    // 无论是否异常都执行（资源释放）
}

// try-with-resources（JDK7+）
try (FileInputStream fis = new FileInputStream("file.txt")) {
    // 自动关闭资源
} catch (IOException e) {
    e.printStackTrace();
}
```

#### 3. 自定义异常

```java
// 编译时异常
public class BusinessException extends Exception {
    public BusinessException(String message) {
        super(message);
    }
}

// 运行时异常
public class ValidationException extends RuntimeException {
    public ValidationException(String message) {
        super(message);
    }
}

// 使用
public void withdraw(double amount) throws BusinessException {
    if (amount > balance) {
        throw new BusinessException("余额不足");
    }
    balance -= amount;
}
```

---

##### 二、泛型（Generics）

#### 1. 泛型的作用

- **类型安全**：编译时检查类型，避免运行时 ClassCastException
- **消除强制转换**：自动类型推断
- **代码复用**：编写通用算法

#### 2. 泛型类

```java
// 定义泛型类
public class Box<T> {
    private T content;
    
    public void setContent(T content) {
        this.content = content;
    }
    
    public T getContent() {
        return content;
    }
}

// 使用
Box<String> stringBox = new Box<>();
stringBox.setContent("Hello");
String content = stringBox.getContent();  // 无需强制转换

Box<Integer> intBox = new Box<>();
intBox.setContent(100);
```

#### 3. 泛型方法

```java
public class GenericMethod {
    // 泛型方法
    public <T> void printArray(T[] array) {
        for (T element : array) {
            System.out.println(element);
        }
    }
    
    // 泛型方法带返回值
    public <T> T getFirst(T[] array) {
        return array.length > 0 ? array[0] : null;
    }
}
```

#### 4. 泛型通配符

| 通配符 | 含义 | 示例 |
|--------|------|------|
| `?` | 任意类型 | `List<?>` |
| `? extends T` | T 或 T 的子类 | `List<? extends Number>` |
| `? super T` | T 或 T 的父类 | `List<? super Integer>` |

```java
// 上界通配符（只读）
public void printNumbers(List<? extends Number> list) {
    for (Number n : list) {
        System.out.println(n);
    }
    // list.add(1);  // 编译错误，无法确定具体类型
}

// 下界通配符（可写）
public void addIntegers(List<? super Integer> list) {
    list.add(1);
    list.add(2);
    // Integer i = list.get(0);  // 编译错误，可能是 Object
}
```

#### 5. 泛型擦除

Java 泛型是编译时特性，运行时类型信息被擦除：

```java
List<String> strList = new ArrayList<>();
List<Integer> intList = new ArrayList<>();

System.out.println(strList.getClass() == intList.getClass());  // true
// 运行时都是 ArrayList，没有泛型信息
```

---

##### 三、List 集合

#### 1. List 接口特点

- **有序**：元素按插入顺序排列
- **可重复**：允许存储重复元素
- **有索引**：支持通过索引访问元素

#### 2. ArrayList vs LinkedList

| 特性 | ArrayList | LinkedList |
|------|-----------|------------|
| 底层结构 | 数组 | 双向链表 |
| 随机访问 | O(1) | O(n) |
| 插入/删除 | O(n) | O(1) |
| 内存占用 | 较少 | 较多 |
| 适用场景 | 查询多、修改少 | 增删多、查询少 |

#### 3. List 常用方法

```java
List<String> list = new ArrayList<>();

// 添加
list.add("Apple");
list.add(0, "Banana");  // 指定位置插入
list.addAll(Arrays.asList("Cherry", "Date"));

// 获取
String first = list.get(0);

// 修改
list.set(0, "Apricot");

// 删除
list.remove("Apple");     // 删除指定元素
list.remove(0);           // 删除指定位置

// 查询
boolean contains = list.contains("Apple");
int index = list.indexOf("Apple");
int size = list.size();
boolean isEmpty = list.isEmpty();

// 遍历
// 方式1：for 循环
for (int i = 0; i < list.size(); i++) {
    System.out.println(list.get(i));
}

// 方式2：增强 for
for (String s : list) {
    System.out.println(s);
}

// 方式3：迭代器
Iterator<String> it = list.iterator();
while (it.hasNext()) {
    System.out.println(it.next());
}

// 方式4：forEach + Lambda
list.forEach(System.out::println);

// 方式5：Stream
list.stream().forEach(System.out::println);
```

#### 4. List 排序

```java
List<Integer> numbers = Arrays.asList(3, 1, 4, 1, 5, 9, 2, 6);

// 自然排序
Collections.sort(numbers);

// 自定义排序
Collections.sort(numbers, (a, b) -> b - a);  // 降序

// 使用 Comparator
numbers.sort(Comparator.naturalOrder());     // 升序
numbers.sort(Comparator.reverseOrder());     // 降序

// 对象排序
List<Student> students = new ArrayList<>();
students.sort(Comparator.comparing(Student::getAge));
students.sort(Comparator.comparing(Student::getAge).reversed());
```

---

##### 四、Collections 工具类

```java
List<Integer> list = new ArrayList<>();

// 排序
Collections.sort(list);
Collections.sort(list, Comparator.reverseOrder());

// 查找
int index = Collections.binarySearch(list, 5);  // 二分查找（需有序）
int max = Collections.max(list);
int min = Collections.min(list);
int frequency = Collections.frequency(list, 5);  // 出现次数

// 替换/填充
Collections.replaceAll(list, 1, 100);  // 替换所有 1 为 100
Collections.fill(list, 0);             // 全部填充为 0

// 反转/打乱
Collections.reverse(list);
Collections.shuffle(list);             // 随机打乱

// 同步集合
List<String> syncList = Collections.synchronizedList(new ArrayList<>());

// 不可变集合
List<String> unmodifiableList = Collections.unmodifiableList(list);
```

---

#### 问答

##### Q1：运行时异常和编译时异常有什么区别？

**答**：
- **运行时异常（RuntimeException）**：编译器不强制处理，通常是编程错误，如空指针、数组越界。
- **编译时异常**：编译器强制要求处理，通常是外部因素导致，如 IO 异常、SQL 异常。
- **处理建议**：运行时异常通过完善代码避免，编译时异常必须显式处理。

##### Q2：泛型擦除会带来什么问题？

**答**：
- 运行时无法获取泛型类型信息（`T.class` 编译错误）。
- 无法创建泛型数组（`new T[10]` 编译错误）。
- 泛型类型在反射时丢失。
- **解决方案**：使用 `Class<T>` 传递类型信息，或使用通配符。

##### Q3：ArrayList 和 LinkedList 如何选择？

**答**：
- **ArrayList**：随机访问频繁、增删操作少、内存敏感的场景。
- **LinkedList**：频繁增删、需要实现队列/栈、不需要随机访问的场景。
- **注意**：大多数场景 ArrayList 性能更好，LinkedList 仅在特定场景有优势。

##### Q4：为什么要使用泛型？

**答**：
- **类型安全**：编译时检查类型，避免运行时 ClassCastException。
- **消除强制转换**：自动类型推断，代码更简洁。
- **代码复用**：编写通用算法，适用于多种类型。
- **可读性**：代码意图更清晰。

##### Q5：try-with-resources 有什么优势？

**答**：
- **自动关闭资源**：无需手动调用 `close()`。
- **异常处理更完善**：资源关闭异常不会掩盖业务异常。
- **代码更简洁**：减少 finally 代码块。
- **要求**：资源必须实现 `AutoCloseable` 接口。

---

> **学习建议**：异常处理、泛型和集合是 Java 进阶的必备知识。异常处理让程序更健壮，泛型让代码更通用，集合让数据操作更高效。建议多练习，熟练掌握这些核心 API。
