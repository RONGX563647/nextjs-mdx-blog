##### 02 🔧 Java Day02 - 方法、类型转换、运算符

> 💡 **本文聚焦 Java 基础语法进阶——方法、类型转换、运算符，帮你夯实基础，避开常见坑。**

---

#### 🎯 快速回顾

- **📦 方法**：封装功能的代码块，支持重载（同名不同参），提高代码复用性
- **🔄 类型转换**：小范围→大范围自动转，大范围→小范围强制转（可能丢失精度）
- **➕ 运算符**：算术、赋值、比较、逻辑、三元运算符
- **⌨️ Scanner**：用于接收键盘输入，需导包 `import java.util.Scanner`

---

#### 📑 目录

- [一、方法](#一方法)
  - [1. 方法的定义](#1-方法的定义)
  - [2. 方法重载](#2-方法重载)
  - [3. 方法示例](#3-方法示例)
- [二、类型转换](#二类型转换)
  - [1. 类型范围](#1-类型范围)
  - [2. 自动转换（小→大）](#2-自动转换小大)
  - [3. 强制转换（大→小）](#3-强制转换大小)
  - [4. 表达式提升](#4-表达式提升)
- [三、运算符](#三运算符)
  - [1. 算术运算符](#1-算术运算符)
  - [2. 赋值运算符](#2-赋值运算符)
  - [3. 比较运算符](#3-比较运算符)
  - [4. 逻辑运算符](#4-逻辑运算符)
  - [5. 三元运算符](#5-三元运算符)
- [四、输入输出](#四输入输出)
  - [1. 输出语句](#1-输出语句)
  - [2. 输入（Scanner）](#2-输入scanner)
- [五、综合案例](#五综合案例)
- [❓ 问答](#问答)

---

#### 📖 详细内容

##### 一、方法

#### 1. 方法的定义

```java
修饰符 返回值类型 方法名(参数列表) {
    方法体;
    return 返回值;  // void 方法可省略
}
```

> **💡 方法的好处**：
> - 提高代码复用性
> - 便于维护和修改
> - 使程序结构更清晰

---

#### 2. 方法重载

**⚙️ 规则**：同一类中，方法名相同，参数列表不同（个数/类型/顺序）

```java
public class Demo {
    public static void print(int num) {
        System.out.println("整数：" + num);
    }

    public static void print(String str) {
        System.out.println("字符串：" + str);
    }

    public static void print(double num, String desc) {
        System.out.println(desc + "：" + num);
    }
}
```

**📊 方法重载匹配流程图：**

```
┌─────────────────────────────────────────────────────────────┐
│                  方法重载匹配流程                    │
│                                                             │
│   调用方法：print(3.14, "圆周率")                           │
│                                                             │
│   ┌─────────────────────────────────────────────┐          │
│   │         查找同名方法 print()                  │          │
│   └──────────────────┬──────────────────────────┘          │
│                      │                                       │
│                      ▼                                       │
│   ┌─────────────────────────────────────────────┐          │
│   │   print(int num)                              │          │
│   │   参数类型：int                               │          │
│   │   匹配：❌ 类型不匹配                          │          │
│   └──────────────────┬──────────────────────────┘          │
│                      │                                       │
│                      ▼                                       │
│   ┌─────────────────────────────────────────────┐          │
│   │   print(String str)                           │          │
│   │   参数类型：String                            │          │
│   │   匹配：❌ 参数个数不匹配                      │          │
│   └──────────────────┬──────────────────────────┘          │
│                      │                                       │
│                      ▼                                       │
│   ┌─────────────────────────────────────────────┐          │
│   │   print(double num, String desc)             │          │
│   │   参数类型：double, String                   │          │
│   │   匹配：✓ 完全匹配                           │          │
│   └──────────────────┬──────────────────────────┘          │
│                      │                                       │
│                      ▼                                       │
│   ┌─────────────────────────────────────────────┐          │
│   │           执行此方法                         │          │
│   │   输出：圆周率：3.14                         │          │
│   └─────────────────────────────────────────────┘          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

> **⚠️ 注意事项**：
> - 方法重载与返回值类型无关
> - 参数列表必须不同（个数、类型或顺序）
> - 不能仅通过返回值类型区分重载方法

---

#### 3. 方法示例

```java
public class Calculator {
    // 无参无返回
    public void printHello() {
        System.out.println("Hello");
    }

    // 有参有返回
    public int add(int a, int b) {
        return a + b;
    }

    // 重载
    public double add(double a, double b) {
        return a + b;
    }
}
```

---

##### 二、类型转换

#### 1. 类型范围

```
byte(1B) < short(2B) < char(2B) < int(4B) < long(8B) < float(4B) < double(8B)
```

**📊 数据类型范围与转换关系图：**

```
┌─────────────────────────────────────────────────────────────┐
│              Java 数据类型范围与转换关系            │
│                                                             │
│   小范围 ──────────────────────────────────────► 大范围     │
│                                                             │
│   ┌──────┐    ┌──────┐    ┌──────┐    ┌──────┐            │
│   │ byte │    │ short│    │ char │    │ int  │            │
│   │ 1字节│    │ 2字节│    │ 2字节│    │ 4字节│            │
│   └──┬───┘    └──┬───┘    └──┬───┘    └──┬───┘            │
│      │           │           │           │                  │
│      └───────────┴───────────┴───────────┘                  │
│                  │  自动转换（隐式）  │                      │
│                  ▼                    ▼                      │
│   ┌─────────────────────────────────────────────┐          │
│   │              long  8字节                    │          │
│   └──────────────────┬────────────────────────┘          │
│                      │                                     │
│                      ▼                                     │
│   ┌─────────────────────────────────────────────┐          │
│   │              float 4字节                    │          │
│   └──────────────────┬────────────────────────┘          │
│                      │                                     │
│                      ▼                                     │
│   ┌─────────────────────────────────────────────┐          │
│   │              double 8字节                   │          │
│   └─────────────────────────────────────────────┘          │
│                                                             │
│   ◄─────────────────────────────────────────────────────►  │
│           强制转换（显式）可能丢失精度                      │
└─────────────────────────────────────────────────────────────┘
```

---

#### 2. 自动转换（小→大）

```java
int i = 100;
long l = i;  // 自动转换

double d = i;  // 自动转换
```

> **💡 自动转换规则**：
> - 小范围类型可以自动转换为大范围类型
> - 不会丢失数据
> - 编译器自动完成

---

#### 3. 强制转换（大→小）

```java
double d = 99.9;
int i = (int) d;  // 99，截断小数

long l = 10000000000L;
int j = (int) l;  // 可能溢出
```

> **⚠️ 强制转换注意事项**：
> - 可能丢失精度（浮点转整数）
> - 可能溢出（大数转小范围）
> - 建议先检查范围再转换

---

#### 4. 表达式提升

```java
byte b = 10;
short s = 20;
int result = b + s;  // byte/short 运算时提升为 int
```

> **💡 表达式提升规则**：
> - byte、short、char 参与运算时自动提升为 int
> - 不同类型运算时，自动提升为范围较大的类型
> - 浮点运算时，float 提升为 double

---

##### 三、运算符

#### 1. 算术运算符

| 运算符 | 功能 | 示例 |
|:--------|:------|:------|
| `+` | 加法/字符串连接 | `5 + 3 = 8` |
| `-` | 减法 | `5 - 3 = 2` |
| `*` | 乘法 | `5 * 3 = 15` |
| `/` | 除法 | `5 / 2 = 2` |
| `%` | 取模 | `5 % 2 = 1` |
| `++` | 自增 | `a++`、`++a` |
| `--` | 自减 | `a--`、`--a` |

> **💡 注意**：
> - 整数除法会截断小数部分
> - 字符串连接使用 `+`
> - 自增自减有前缀和后缀之分

---

#### 2. 赋值运算符

```java
int a = 10;
a += 5;   // a = a + 5
a -= 3;   // a = a - 3
a *= 2;   // a = a * 2
a /= 2;   // a = a / 2
a %= 3;   // a = a % 3
```

> **💡 赋值运算符特点**：
> - 简化复合运算表达式
> - 自动进行类型转换
> - 代码更简洁易读

---

#### 3. 比较运算符

| 运算符 | 功能 | 示例 |
|:--------|:------|:------|
| `==` | 等于 | `5 == 5` → `true` |
| `!=` | 不等于 | `5 != 3` → `true` |
| `>` | 大于 | `5 > 3` → `true` |
| `<` | 小于 | `5 < 3` → `false` |
| `>=` | 大于等于 | `5 >= 5` → `true` |
| `<=` | 小于等于 | `5 <= 3` → `false` |

> **⚠️ 注意**：
> - 比较运算符结果为 `boolean` 类型
> - 比较引用类型时使用 `equals()` 而非 `==`

---

#### 4. 逻辑运算符

| 运算符 | 功能 | 特点 |
|:--------|:------|:------|
| `&&` | 短路与 | 左边 `false`，右边不执行 |
| `||` | 短路或 | 左边 `true`，右边不执行 |
| `!` | 非 | 取反 |

> **💡 短路运算符优势**：
> - 提高程序效率
> - 避免不必要的计算
> - 可用于空值检查

---

#### 5. 三元运算符

```java
// 语法
条件 ? 值1 : 值2;

// 示例
int max = a > b ? a : b;
String result = num % 2 == 0 ? "偶数" : "奇数";
```

> **💡 三元运算符特点**：
> - 简化 if-else 语句
> - 必须返回一个值
> - 可嵌套使用（但不推荐）

---

##### 四、输入输出

#### 1. 输出语句

```java
System.out.println("Hello");  // 打印后换行
System.out.print("Hello");    // 打印不换行
System.out.printf("%.2f", 3.14159);  // 格式化输出
```

> **💡 输出方法对比**：
> - `println()`：输出后换行
> - `print()`：输出不换行
> - `printf()`：格式化输出，类似 C 语言

---

#### 2. 输入（Scanner）

```java
import java.util.Scanner;

public class InputDemo {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        System.out.print("请输入年龄：");
        int age = sc.nextInt();

        System.out.print("请输入姓名：");
        String name = sc.next();

        System.out.print("请输入体重：");
        double weight = sc.nextDouble();

        sc.close();

        System.out.println("姓名：" + name + "，年龄：" + age + "，体重：" + weight);
    }
}
```

> **⚠️ Scanner 使用注意**：
> - 记得关闭 `Scanner` 对象
> - 输入类型要与方法匹配
> - 处理输入异常（如输入非数字）

---

##### 五、综合案例

```java
import java.util.Scanner;

public class HealthCalculator {
    // BMI 计算
    public static double calculateBMI(double weight, double height) {
        double heightM = height / 100;
        return weight / (heightM * heightM);
    }

    // BMI 等级判断
    public static String getBMICategory(double bmi) {
        return bmi < 18.5 ? "体重过低" :
               bmi < 25 ? "正常范围" :
               bmi < 30 ? "超重" : "肥胖";
    }

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        System.out.print("体重(kg)：");
        double weight = sc.nextDouble();

        System.out.print("身高(cm)：");
        double height = sc.nextDouble();

        sc.close();

        double bmi = calculateBMI(weight, height);
        System.out.printf("BMI：%.2f（%s）\n", bmi, getBMICategory(bmi));
    }
}
```

> **💡 案例说明**：
> - 综合运用方法、类型转换、运算符
> - 实际应用场景：健康计算器
> - 练习输入输出和格式化输出

---

#### ❓ 问答

##### Q1：方法重载和方法重写有什么区别？

**答**：
- **重载**：同一类中，方法名相同，参数列表不同。
- **重写**：子类对父类方法的重新实现，方法签名完全相同。

---

##### Q2：强制类型转换可能产生什么问题？

**答**：
- **数据溢出**：大范围转小范围时，高位被截断。
- **精度丢失**：浮点转整数时，小数部分被截断。

---

##### Q3：`&&` 和 `&` 有什么区别？

**答**：
- `&&`（短路与）：左边为 `false`，右边不执行。
- `&`（非短路与）：两边都执行。
- **推荐**：使用 `&&`，效率更高。

---

##### Q4：`a++` 和 `++a` 有什么区别？

**答**：
- `a++`（后缀）：先使用当前值，再自增。
- `++a`（前缀）：先自增，再使用新值。

---

##### Q5：整数除法和浮点数除法有什么区别？

**答**：
- **整数除法**：`5 / 2 = 2`（截断小数）。
- **浮点数除法**：`5.0 / 2 = 2.5`。

---

> **📚 学习建议**：本节内容是 Java 编程的基础，务必理解方法的设计思想、类型转换的规则、运算符的优先级。建议多写练习，特别是方法重载和运算符的综合应用。
